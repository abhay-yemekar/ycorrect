{
  "name": "ycorrect",
  "version": "0.2.0",
  "private": true,
  "description": "A personal writing assistant with grammar checking and AI rewriting — WriteRight.",
  "type": "module",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "start": "node server/app.js",
    "dev": "node server/app.js",
    "test": "node --test \"tests/unit/**/*.test.js\"",
    "lint": "eslint .",
    "check": "npm run lint && npm test",
    "build": "node scripts/package-extension.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@eslint/js": "^9.30.0",
    "eslint": "^9.30.0"
  }
}
